# ── Frontend (Vite) ──────────────────────────────────────────────
# URL of the backend API server
VITE_API_URL=http://localhost:3001

# ── Backend ──────────────────────────────────────────────────────
# Port the Express server listens on
PORT=3001

# Stellar Horizon endpoint (Testnet)
HORIZON_URL=https://horizon-testnet.stellar.org

# Allowed frontend origin for CORS
FRONTEND_URL=http://localhost:4173

# Optional bearer token protecting POST /api/ingest/run (empty = open)
ADMIN_TOKEN=

# ── Soroban / Smart Contracts (live Testnet deployment) ──────────
# Deployed bounty contract (see docs/DEPLOYMENT.md)
BOUNTY_CONTRACT_ID=CCVDE7Q3UF4O223ONMLUPYPUZWFO7STOQJWCDD3C5LELDLU4GHOFLCH7

# Deployed RRD demo token (Soroban Asset Contract)
DEMO_TOKEN_ID=CCOAF5DIHLO4457S6EGQYSLXGGDRQPTO42DU6N5KVH4M2MSA2VDW2NB4

# Deployed contributors registry contract (inter-contract communication)
CONTRIBUTOR_REGISTRY_ID=CBIA55MJABCVVMZ6BMF3GNZ7USTNJW2ZJQMO2MNUXHFPRN34JOOES3CS

# Public admin address (release/reclaim gate). The secret is NEVER set here.
BOUNTY_ADMIN_ADDRESS=GBRVOQSLP32BGOCYA56DCTM5PUQWW7YLBBAKVXBHDTJ6SNKVLGMWFRQI

# Soroban RPC endpoint
SOROBAN_RPC_URL=https://soroban-testnet.stellar.org

# Network passphrase
STELLAR_NETWORK_PASSPHRASE=Test SDF Network ; September 2015

# ── Ingestion / Indexer ──────────────────────────────────────────
# Optional GitHub repo to ingest open issues from (empty = use seed data)
GITHUB_REPO=
GITHUB_TOKEN=

# Ingest on boot when tables are empty (true/false)
INGEST_ON_START=true
# Force re-ingest on every boot
REINGEST_ON_START=false

# Poll the contract for new events (true/false) and how often (ms)
INDEXER_ENABLED=true
INDEXER_INTERVAL_MS=15000
# Start indexing from this ledger (0 = from genesis of the contract)
INDEXER_START_LEDGER=0

# ── Observability ────────────────────────────────────────────────
LOG_LEVEL=info
# Per-IP rate limit: window ms + max requests
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX=120